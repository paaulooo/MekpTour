{
    "version": 2,
    "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
    "builds": [
        {
        "src": "./index.html",
        "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
